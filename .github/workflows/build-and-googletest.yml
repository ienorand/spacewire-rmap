name: Build library and test via googletest

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build_and_test:

    runs-on: ubuntu-latest

    steps:
    - name: install googletest
      run: sudo apt install googletest
    - uses: actions/checkout@v3
    - name: build library
      run: make
    - name: build and run googletest
      run: make test
